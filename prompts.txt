2026-03-29T00:00:00Z | Created CLAUDE.md in the project root with prompt logging instructions.
2026-03-29T00:01:00Z | Scaffolded full Feature Voting System: Docker Compose (db/backend/frontend), Django 5+DRF with users/features/votes apps (venv+django-admin), Next.js 14 App Router with TypeScript/Tailwind/shadcn, fake X-User-ID auth, vote_count signals, .env.example files, README.md.
2026-03-29T00:02:00Z | Fixed CORS (X-User-ID custom header blocked preflight; switched to Authorization: Token) and implemented proper auth structure: DRF TokenAuthentication, POST /api/auth/login/ + GET /api/auth/me/ endpoints, React AuthContext with login/user/loading, auto-login on first visit, UserSwitcher uses context. Removed user.ts.
2026-03-29T00:03:00Z | Added self-vote prevention (backend 403 + is_author serializer field), disabled VoteButton with dashed style for own features, added #rank numbers to list page sorted by popularity.
2026-03-29T00:04:00Z | UX/auth improvements: added /login page (name-only, no password), removed auto-UUID login from AuthContext, added NavBar client component with auth guard (redirect to /login) and current user display in header, updated UserSwitcher to use soft navigation (router.push) instead of page reload and show full username.
2026-03-29T00:05:00Z | Switched to real JWT auth with djangorestframework-simplejwt: replaced DRF TokenAuthentication with JWTAuthentication, removed custom LoginView in favor of TokenObtainPairView, added seed_users management command, updated frontend to use Bearer header and password-based login, added logout to AuthContext and NavBar, removed UserSwitcher.

[2026-03-30T00:00:00Z] Invoked /frontend-design skill. Redesigned home page and FeatureCard with a dark "Broadcast Board" aesthetic: deep navy-black background, amber/gold (#primary = oklch(0.82 0.18 82)) accent, Sora + Bebas Neue fonts, horizontal feature strips with faint rank watermarks, amber VoteButton counter, staggered fade-slide-up entrance animations.

[2026-03-31T00:00:00Z] Removed unused IsAuthenticated import from features/views.py. Replaced prefetch_related('votes') + Python iteration for has_voted with a SQL Exists subquery annotation; switched to get_queryset() to inject request.user at query time; simplified serializer has_voted to BooleanField(read_only=True). 
